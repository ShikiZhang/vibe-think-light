name: Software tests
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Unit tests, app build, CLI validation (no USB writes)
        run: ./test.sh
