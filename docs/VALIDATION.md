# 发布前验证记录

本记录区分这次公开源码构建与此前实体设备验证，不把模拟测试当作灯珠或生命周期证据。

## 本次：2026-09-09

环境：Apple Silicon macOS 26；Xcode Command Line Tools / Swift 6.2.3；系统 Python 3；Arm GCC 14.2.1。仅此环境完成验证，最低部署版本 macOS 13 和 Intel 运行效果尚未实测。

| 检查 | 结果与范围 |
| --- | --- |
| `./test.sh` | PASS：C 固件状态机 + UndefinedBehaviorSanitizer、Swift 协议、8 个 hook 测试、3 个安装器测试、Mac app 编译与 plist/签名、42 效果及 8 组 CLI 无效输入 |
| 安装器 | PASS：在含空格的临时路径合并、重复安装、卸载、保留原有 hook/设置/config；未安装到真实 Codex home |
| QMK 固件 | PASS：从官方 Git 固定提交重新下载源码和 4 个所需子模块；全新独立 venv；工具链使用已下载的官方归档并重新核对 SHA-256 |
| 固件一致性 | 37,696 字节（含 16 字节 DFU suffix），SHA-256 `97dbdb92b4282036236231dbd34f3aead0bcdf70da963b64da21d5ffa53010ab`，与此前已刷入并验证的 v1.2 二进制完全相同 |
| 适配范围 | 公开版本程序增加 V3 VID/PID 限定，同时要求专属 usage；仅识别目标型号 |
| 真机只读 | 使用公开构建的 CLI GET 核对固件当前状态和调速能力；不重新刷机 |

测试覆盖：坏报文、错误参数、稳定效果 ID、亮度上限、无隐式 EEPROM 写入、保存/重载、通知替换、超时恢复、计时回绕、手动取消、Caps Lock 恢复、7 种通知样式、6 种自定义周期和边界值、序号匹配、旧版能力回退、异步等待与跨任务提醒优先级。

## 此前已在同一 Think6.5 V3 验证

- v1.2 固件真实刷入成功；Enter、Fn、原生灯效工作正常。
- GET/SET 确认和回读、临时通知以及自动恢复通过真实 USB 检查。
- 7 种通知和 6 种可调周期动画通过真实 USB 参数/恢复检查；绿色呼吸、黄色双闪由使用者肉眼确认。
- Codex 完全退出重启后，真实 Stop hook 执行成功且使用者看到提醒。此前未重启时仅模拟调用成功，实际生命周期没有触发；文档保留了这一步骤。

本次开源整理未覆盖安装中的 app、用户 hook 设置或重新刷实体固件；不能据此声称在其他人的 Codex 版本、Mac 或键盘配列上也已验证。公共仓库 CI 状态请以 GitHub Actions 实际记录为准。
